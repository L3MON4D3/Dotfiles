-- collect functions for identifying structures in code.
-- These may use manual parsing, or treesitter, but must return a common format:
-- {
-- 	type = [string],
-- 	range = {from, to},
-- 	data = [type-specific]
-- }

return {
	
}
